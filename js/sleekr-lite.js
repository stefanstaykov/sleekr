(function( $ ) {
    "use strict";
 
    $(function() {
        if ( $( "#wpadminbar" ).length ) {
            $( ".navbar" ).addClass('admin-bar');
        }
        $("img").addClass('img-responsive');
    });
 
})( jQuery );

